import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.idxgaza.traneem',
  appName: 'ترانيم',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
